{
  "name": "todo-api",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "author": "Dennis Zimmermann (dennzimm)",
  "license": "MIT",
  "engines": {
    "node": "9.4.0"
  },
  "scripts": {
    "start": "node app.js",
    "dev": "export NODE_ENV=development || SET \"NODE_ENV=development\" && nodemon app.js",
    "test": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha tests/**/*.test.js",
    "test-watch": "export NODE_ENV=test || SET \"NODE_ENV=test\" && nodemon --exec \"npm test \""
  },
  "devDependencies": {
    "eslint": "^4.17.0",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.8.0",
    "expect": "^22.1.0",
    "mocha": "^5.0.0",
    "nodemon": "^1.14.12",
    "supertest": "^3.0.0"
  },
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.2",
    "lodash": "^4.17.5",
    "mongodb": "^3.0.2",
    "mongoose": "^5.0.3",
    "validator": "^9.4.0"
  }
}
